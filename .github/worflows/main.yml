on: [push]

jobs:
  hellow_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - name: Hellow world action step
        id: hello
        uses: ./
        with:
          who-to-greet: 'Mona'
      - name: Get te output time
        run: echo "The time was ${{ steps.hello.output.time }}"